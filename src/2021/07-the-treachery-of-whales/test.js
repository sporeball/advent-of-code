const submarines = require('./submarines');
const submarines2 = require('./submarines2');

console.log(`optimal fuel usage: ${submarines()}`);
console.log(`optimal fuel usage (revised): ${submarines2()}`);

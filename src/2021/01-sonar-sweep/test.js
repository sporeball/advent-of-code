const counter = require('./counter');
const counter2 = require('./counter2');

console.log(`the measurements increased ${counter()} times`);
console.log(`the sliding sums increased ${counter2()} times`);

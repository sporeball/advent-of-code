const calculator = require('./calculator');
const calculator2 = require('./calculator2');

console.log(`final x-position times y-position = ${calculator()}`);
console.log(`final x-position times y-position (revised) = ${calculator2()}`);
